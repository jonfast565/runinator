[package]
name = "runinator-plugin"
version.workspace = true
edition = "2024"

[dependencies]
log = "0.4"
libloading = "0.9.0"
serde_json = "1.0"

runinator-utilities = { path = "../runinator-utilities" }
runinator-models = { path = "../runinator-models" }

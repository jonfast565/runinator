[package]
name = "runinator-console"
version.workspace = true
edition = "2024"

[dependencies]
uuid = { version = "1", features = ["v4"] }

runinator-models = { path = "../runinator-models" }
runinator-rexrap = { path = "../runinator-rexrap" }

[package]
name = "runinator-provider-git"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

runinator-plugin = { path = "../runinator-plugin" }
runinator-models = { path = "../runinator-models" }

[package]
name = "runinator-workflows"
version = "0.1.0"
edition = "2024"

[dependencies]
runinator-compute = { path = "../runinator-compute" }
runinator-models = { path = "../runinator-models" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
uuid = { version = "1.18", features = ["serde", "v4", "v7"] }

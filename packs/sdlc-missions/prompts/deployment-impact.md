# Assess deployment impact

Inspect the committed diff and repository deployment guidance. Select deployment only when the
configured named deployment workflow is required for these changes. Do not edit files, dispatch,
deploy, or change credentials. Return the response required by the supplied JSON schema.

{
  "name": "runinator-rexrap",
  "displayName": "Runinator REXRAP",
  "description": "Diagnostics, completion, hover, formatting, and apply-on-save for Runinator REXRAP workflows.",
  "version": "0.5.547",
  "publisher": "runinator",
  "engines": {
    "vscode": "^1.75.0"
  },
  "categories": [
    "Programming Languages"
  ],
  "activationEvents": [
    "onLanguage:rexrap"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "languages": [
      {
        "id": "rexrap",
        "aliases": [
          "Runinator REXRAP",
          "rexrap"
        ],
        "extensions": [
          ".rrx"
        ],
        "configuration": "./language-configuration.json"
      }
    ],
    "grammars": [
      {
        "language": "rexrap",
        "scopeName": "source.rexrap",
        "path": "./syntaxes/rexrap.tmLanguage.json"
      }
    ],
    "snippets": [
      {
        "language": "rexrap",
        "path": "./snippets/rexrap.json"
      }
    ],
    "configuration": {
      "title": "Runinator REXRAP",
      "properties": {
        "runinator.serverPath": {
          "type": "string",
          "default": "runinator-lsp",
          "description": "Path to the runinator-lsp server binary."
        },
        "runinator.apiBaseUrl": {
          "type": "string",
          "default": "http://127.0.0.1:8080/",
          "description": "Web service base URL used to fetch provider/setting metadata for completion (RUNINATOR_API_BASE_URL)."
        },
        "runinator.autoApply": {
          "type": "boolean",
          "default": false,
          "description": "On save, compile the workflow pack and import it into the configured service (overwrite)."
        },
        "runinator.serviceUrl": {
          "type": "string",
          "default": "",
          "description": "Web service base URL that apply-on-save imports into. Defaults to runinator.apiBaseUrl when empty."
        }
      }
    }
  },
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "dependencies": {
    "vscode-languageclient": "^9.0.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.75.0",
    "typescript": "^5.4.0"
  }
}

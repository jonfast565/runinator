[package]
name = "runinator-db-cli"
version.workspace = true
edition = "2024"
license = "Apache-2.0"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
runinator-database = { path = "../runinator-database" }
